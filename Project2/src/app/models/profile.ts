export class Profile {
    constructor(private profileImage:string, private bio:string, private galaxy:string,
                private solarSystem:string, private planet:string, private email:string) {}
}
